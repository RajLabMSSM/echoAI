% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SPLICEAI_run.R
\name{SPLICEAI_run}
\alias{SPLICEAI_run}
\title{Run pre-trained \emph{SpliceAI} model}
\source{
\url{https://github.com/Illumina/SpliceAI}
\doi{10.1016/j.cell.2018.12.015}
}
\usage{
SPLICEAI_run(
  vcf_path = "./GWAS_converted.vcf",
  output_path = "spliceai_predictions.vcf",
  reference_fasta,
  gene_annotation = "grch37",
  distance = 50,
  mask = 0
)
}
\arguments{
\item{vcf_path}{Path to the input VCF file.}

\item{output_path}{Path to the output VCF file.}

\item{reference_fasta}{Path to the reference genome FASTA file
(must be unzipped).}

\item{gene_annotation}{Path to a gene annotation file, or one of
\code{"grch37"} / \code{"grch38"} to use the GENCODE canonical
annotations bundled with SpliceAI.}

\item{distance}{Maximum distance (in bases) between the variant and a
gained/lost splice site. Defaults to 50.}

\item{mask}{Integer flag controlling score masking. When set to 1,
masks scores representing annotated acceptor/donor gain and
unannotated acceptor/donor loss. Defaults to 0 (no masking).}
}
\value{
The exit code from the \code{spliceai} system call
  (invisibly).
}
\description{
Run the pre-trained \emph{SpliceAI} deep learning model to predict
splice-site alterations from genetic variants. Requires the
\code{spliceai} command-line tool to be installed and on the system
\code{PATH} (e.g. via \code{pip install spliceai}).
}
\examples{
\dontrun{
SPLICEAI_run(
    vcf_path = "variants.vcf",
    output_path = "spliceai_predictions.vcf",
    reference_fasta = "hg19.fa",
    gene_annotation = "grch37"
)
}
}
\seealso{
Other SPLICEAI: 
\code{\link{SPLICEAI_plot}()},
\code{\link{SPLICEAI_query_tsv}()},
\code{\link{SPLICEAI_query_tsv_iterate}()},
\code{\link{SPLICEAI_query_vcf}()},
\code{\link{SPLICEAI_snp_probs}()}
}
\concept{SPLICEAI}
