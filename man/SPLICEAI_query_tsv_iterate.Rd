% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SPLICEAI_query.R
\name{SPLICEAI_query_tsv_iterate}
\alias{SPLICEAI_query_tsv_iterate}
\title{Iterate \emph{SpliceAI} TSV queries across multiple loci}
\source{
\url{https://github.com/Illumina/SpliceAI}
\doi{10.1016/j.cell.2018.12.015}
}
\usage{
SPLICEAI_query_tsv_iterate(
  sumstats_paths,
  precomputed_path,
  nThread = 1,
  merge_data = TRUE,
  drop_na = TRUE,
  filtered = FALSE,
  save_path = "./spliceAI_subset.tsv.gz"
)
}
\arguments{
\item{sumstats_paths}{Character vector of paths to per-locus
summary-statistics files (TSV / TSV.GZ).}

\item{precomputed_path}{Path to the bgzipped and tabix-indexed
pre-computed SpliceAI TSV file.}

\item{nThread}{Number of threads for \code{\link[parallel]{mclapply}}
and \code{\link[data.table]{fwrite}}.}

\item{merge_data}{Passed to \code{\link{SPLICEAI_query_tsv}}.}

\item{drop_na}{Passed to \code{\link{SPLICEAI_query_tsv}}.}

\item{filtered}{Passed to \code{\link{SPLICEAI_query_tsv}}.}

\item{save_path}{Path to save the combined results, or \code{FALSE}
to skip saving.}
}
\value{
A \code{data.table} of combined SpliceAI results across
  all loci.
}
\description{
Make multiple queries to a genome-wide pre-computed \emph{SpliceAI}
results file (TSV format), one per summary-statistics file.
Results are optionally merged and saved.
}
\examples{
\dontrun{
sumstats_paths <- list.files(
    "path/to/sumstats",
    pattern = "*.tsv.gz",
    recursive = TRUE, full.names = TRUE
)
DAT <- SPLICEAI_query_tsv_iterate(
    sumstats_paths = sumstats_paths,
    precomputed_path = "spliceai_scores.tsv.gz"
)
}
}
\seealso{
Other SPLICEAI: 
\code{\link{SPLICEAI_plot}()},
\code{\link{SPLICEAI_query_api}()},
\code{\link{SPLICEAI_query_tsv}()},
\code{\link{SPLICEAI_query_vcf}()},
\code{\link{SPLICEAI_run}()},
\code{\link{SPLICEAI_snp_probs}()}
}
\concept{SPLICEAI}
